pipeline{
    agent any
    stages {
    stage('checkout scm'){
        steps{
        checkout scm
    }
}
        stage('submit stack') {
            steps {
                bat "aws cloudformation create-stack --stack-name MyStack --template-body file://ec2.yaml --region 'us-east-1'"
            }
        }

    }

}
