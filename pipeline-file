pipeline{
    agent any
    stages {
    stage("Checkout scm")
{
    steps{
          checkout scm
}
}
        stage('submit stack') {
            steps {
                bat "aws cloudformation create-stack --stack-name Vignesh26 --disable-rollback --on-failure DO_NOTHING --debug"
            }
        }

    }

}
