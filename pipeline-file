pipeline{
    agent any
    stages {
    
        stage('submit stack') {
            steps {
                bat "aws cloudformation create-stack --stack-name MyStack1 --template-body file://ec2.yaml --region ap-south-1"
            }
        }

    }

}
